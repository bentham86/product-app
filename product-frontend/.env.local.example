# ──────────────────────────────────────────────────────────────
# Environment variables for the Product Management Frontend
# Copy this file to .env.local and fill in the values.
# ──────────────────────────────────────────────────────────────

# ─── API ──────────────────────────────────────────────────────
# Base URL of the Rails backend (no trailing slash).
# Leave empty or unset to use in-memory mock data.
#
# Development:
# NEXT_PUBLIC_API_BASE_URL=http://localhost:3001
#
# Staging:
# NEXT_PUBLIC_API_BASE_URL=https://api-staging.example.com
#
# Production:
# NEXT_PUBLIC_API_BASE_URL=https://api.example.com
NEXT_PUBLIC_API_BASE_URL=

# Fetch timeout in milliseconds (default: 10000)
# NEXT_PUBLIC_API_TIMEOUT=10000

# ─── Pagination ───────────────────────────────────────────────
# Default items per page (default: 10)
# NEXT_PUBLIC_DEFAULT_PER_PAGE=10

# ─── Mock settings ────────────────────────────────────────────
# Artificial delay (ms) for mock responses (default: 400).
# Set to 0 for instant responses during development.
# NEXT_PUBLIC_MOCK_DELAY=400
